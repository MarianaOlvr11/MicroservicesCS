server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
   config:
     server:
       git:
         uri: https://github.com/MarianaOlvr11/config-server.git
         default-label: main
         search-paths:
           - 'greeting-service*'